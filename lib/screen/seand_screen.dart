import 'package:flutter/material.dart';

class SeandScreen extends StatelessWidget {
  const SeandScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
